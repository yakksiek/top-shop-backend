{
    "name": "json-server",
    "version": "1.0.0",
    "scripts": {
        "start": "json-server --watch db.json --static ./public --port $PORT"
    },
    "dependencies": {
        "json-server": "^0.16.3"
    }
}
